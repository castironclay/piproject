#/etc/wpa_supplicant/wpa_supplicant.conf                                            
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
network={
        ssid="homessid"
        psk="homewirelesskey"
        priority=1
        id_str="home"
}

network={
        ssid="SSID goes here"
        psk="key goes here"
        priority=15
        id_str="phone"
}
